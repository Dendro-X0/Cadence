{
  "name": "@cadence/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight, offline-first focus & motivation PWA (web, desktop-ready via Tauri).",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cadence/core-domain": "file:../../packages/core-domain",
    "@cadence/notifications": "file:../../packages/notifications",
    "@cadence/storage": "file:../../packages/storage",
    "@cadence/templates": "file:../../packages/templates",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "jsdom": "^24.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.10",
    "vite-plugin-pwa": "^1.1.0",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^1.6.0"
  }
}
